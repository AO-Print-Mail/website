import { styled, css } from '@theme'
import { forwardRef } from 'react'

const titleSizes = {}

const Title = styled('h1', {
  color: '$DA100',
  fontWeight: '$semibold',
  fontFamily: '$heading',
  fontSize: '$h1m',
  marginTop: '1em',
  lineHeight: '$1',
})
